# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && PYTHONPATH=/app/backend uvicorn app.main:app --host 0.0.0.0 --port $PORT"